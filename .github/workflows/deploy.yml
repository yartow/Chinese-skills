name: Deploy to Mac Mini

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: git -C ~/Documents/GitHub/chinese-skills pull

      - name: Rebuild and restart Docker containers
        run: cd ~/Documents/GitHub/chinese-skills && docker compose up -d --build
