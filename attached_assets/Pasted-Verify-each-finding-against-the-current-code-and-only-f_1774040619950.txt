Verify each finding against the current code and only fix it if needed.

Inline comments:
In `@client/src/components/FillInBlankQuiz.tsx`:
- Around line 52-80: The onSuccess callback is reading the mutable component
state question! which can change before the async response returns; capture the
submitted question at the moment the prompt is sent and use that captured value
throughout onSuccess instead of question!. Concretely, before invoking the async
submit/mutate call, store const submittedQuestion = question! (or include it in
the mutation vars) and then in onSuccess use submittedQuestion for setResult,
all setScores updates, saveProgress(submittedQuestion.characterIndex,
"reading"), and setWrongAnswers (use vars.answer paired with submittedQuestion
fields) so scoring/progress/wrong-answer logging apply to the original prompt
that was submitted, not the current question state.

In `@client/src/components/HandwritingQuiz.tsx`:
- Around line 232-235: selectCandidate currently only accepts question.character
and question.traditional but ignores question.traditionalVariants; update
selectCandidate to treat any string in question.traditionalVariants as accepted
(in addition to question.character and question.traditional) when computing
isCorrect so alternate traditional forms score as correct; reference the
selectCandidate function and the QuizQuestion fields character, traditional, and
traditionalVariants when making this change.
- Around line 199-226: Pointer coordinates are being passed in CSS pixels
directly into the fixed CANVAS_SIZE drawing space, causing strokes to
stretch/compress; update handleMouseDown, handleMouseMove, handleTouchStart, and
handleTouchMove to normalize CSS/pointer coordinates into the canvas's internal
pixel space by mapping from the element rect to CANVAS_SIZE (e.g., scaleX =
CANVAS_SIZE / rect.width; scaleY = CANVAS_SIZE / rect.height; then apply to the
pointer delta) before calling startStroke/continueStroke; keep the existing DPR
comment in mind and do NOT multiply by DPR when normalizing.

In `@client/src/components/MultipleChoiceQuiz.tsx`:
- Around line 147-150: Compute the new selected-level set once (e.g. const
nextLevels = selectedLevels.includes(level) ? (selectedLevels.length > 1 ?
selectedLevels.filter(l => l !== level) : selectedLevels) : [...selectedLevels,
level].sort()), then call toggleLevel(level) and loadQuestion(nextLevels); this
prevents building [level, level] when the clicked level is the only selected one
and ensures the same computed set is used for both state toggling and fetching.
Reference: QuizShell prop onToggleLevel, toggleLevel, loadQuestion,
selectedLevels.
- Around line 55-71: The loadQuestion function can apply stale async results
when users trigger overlapping calls; add request sequencing to ignore
out-of-order completions: create a per-component mutable request id (e.g., a
useRef counter) that you increment at the start of loadQuestion, capture the id
in the async closure, and before any state updates (setChoices, setQuestion,
setIsError, setIsLoading, setSelected) compare the captured id against the
current ref value and bail out if they differ; alternatively use an
AbortController for the fetchQuestion/fetchChoices calls and abort prior
controller before starting a new load. Target symbols: loadQuestion,
fetchQuestion, fetchChoices and the state setters (setChoices, setQuestion,
setIsError, setIsLoading, setSelected).
- Around line 76-86: In MultipleChoiceQuiz, the useEffect currently lists an
undeclared dependency `result` and allows "N" to skip unanswered questions;
update the effect to depend on `selected` instead of `result`, and modify the
onKey handler (inside useEffect) to early-return unless selected !== null (and
still ignore when an input is focused) before calling handleNext; this ensures
TypeScript compiles and prevents skipping unanswered questions.

In `@client/src/pages/test-mode-page.tsx`:
- Around line 18-36: Tab switches currently unmount quiz components and lose
local state; to preserve state quickly, add Radix's forceMount prop to each
TabsContent containing MultipleChoiceQuiz, FillInBlankQuiz, and HandwritingQuiz
in test-mode-page.tsx so they stay mounted across tab changes (or alternatively
lift each quiz's state up into the parent component via useState and pass
state/handlers down if you prefer a more memory-efficient solution).

In `@server/autoSeed.ts`:
- Around line 14-15: The radicals branch currently skips seeding when any rows
exist (checks radCount.value === 0), which prevents completing partial seeds;
change the guard to compare the current count against the number of intended
seed entries (e.g., use radCount.value < radicalsSeed.length or the existing
seed array name) so seeding runs until count >= seed length, applying the same
pattern used for the other table; keep the idempotent insert strategy
(onConflictDoNothing()) and use the radicals and radCount symbols to locate and
update the condition.
- Around line 8-18: The seeding paths use __dirname (derived from
import.meta.url) which breaks after bundling; update ensureDataSeeded to build
seed file paths from process.cwd() instead of __dirname so the code looks for
server/data/radicals-seed.json and server/data/characters-seed.json relative to
the application root; locate the path construction for radSeedPath (and the
analogous characters seed path around lines handling characters) and replace
path.join(__dirname, "data", ...) with path.join(process.cwd(), "server",
"data", ...) so the bundled app can find the seed files.

In `@server/routes.ts`:
- Around line 629-650: Don't pick a single randomPage before knowing result
size; instead remove randomPage and repeatedly call
storage.getFilteredCharacters (or request a larger POOL_SIZE) to accumulate
characters into the pool until you have enough candidates (aim for at least 3
same-level or at least 3 total) or no more pages; compute sameLevelPool from
that accumulated pool, select up to 3 distractors from sameLevelPool first, and
if fewer than 3, fill the remainder from the accumulated pool excluding already
chosen and the correct character (use the existing symbols POOL_SIZE,
storage.getFilteredCharacters, pool, sameLevelPool, distractors, correctIndex to
locate where to change).
- Around line 567-578: The filter currently allows sentences where
char.simplified appears multiple times and only blanks the first occurrence;
update the examples filter (the anonymous function used to compute valid) to
reject any e.chinese that contains more than one occurrence of char.simplified
by counting occurrences (e.g., split on char.simplified and ensure parts.length
=== 2) before proceeding to blanking; keep the existing checks (length, parts
non-empty, CJK_REGEX on e.english) but add this single-occurrence guard using
e.chinese and char.simplified so a second occurrence cannot leak the answer via
the blanked variable.

---

Outside diff comments:
In `@client/src/components/FillInBlankQuiz.tsx`:
- Around line 95-106: The submit handler allows starting a new check while a
previous one is pending; update handleSubmit to return early if
checkMutation.isPending (or checkMutation.status === 'pending') so repeated
Enter presses don't queue duplicate checks—i.e., add a guard at the top of
handleSubmit referencing checkMutation.isPending before validating
question/answer, and ensure any Enter key handler likewise calls this guarded
handleSubmit to prevent duplicate submissions.