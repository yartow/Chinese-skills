import StrokeOrder from "../StrokeOrder";

export default function StrokeOrderExample() {
  return (
    <div className="p-8 flex items-center justify-center bg-background">
      <StrokeOrder character="学" />
    </div>
  );
}
